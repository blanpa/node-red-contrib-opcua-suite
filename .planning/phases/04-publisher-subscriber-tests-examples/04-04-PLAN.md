---
phase: 04-publisher-subscriber-tests-examples
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - examples/10 - PubSub UDP-UADP Loopback.json
  - examples/11 - PubSub MQTT-UADP.json
  - examples/12 - PubSub MQTT-JSON.json
  - test/run-examples.js
  - test/example-flows.test.js
  - README.md
autonomous: false
requirements:
  - DOC-01
  - DOC-02
user_setup: []

must_haves:
  truths:
    - "examples/ contains three new PubSub flows: '10 - PubSub UDP-UADP Loopback.json', '11 - PubSub MQTT-UADP.json', '12 - PubSub MQTT-JSON.json'"
    - "Each flow is a valid Node-RED flow array (one tab node + opcua-pubsub-connection + opcua-publisher + opcua-subscriber + inject + debug) that imports and deploys without errors"
    - "Flow 10 is a self-contained UDP-UADP loopback (publisher and subscriber share the same multicast group on the same opcua-pubsub-connection) — runnable with NO external infrastructure"
    - "Flows 11 and 12 reference mqtt://localhost:1883; the tab info text states a local MQTT broker (e.g. Mosquitto) is required"
    - "Flow 11 uses messageEncoding='uadp'; flow 12 uses messageEncoding='json'; flow 10 uses messageEncoding='uadp' (UDP-only-UADP rule, D4-03)"
    - "Every property value in the flows matches the real config property names of opcua-pubsub-connection, opcua-publisher and opcua-subscriber (from plans 04-01 / 04-02)"
    - "npm test statically validates flows 10-12 (parse as JSON array, contain a tab, contain the four PubSub node types, every node references a real config-node id) alongside flows 01-09"
    - "test/run-examples.js (live runner) recognizes the new PubSub flows without crashing and no longer assumes every example needs the opcua-endpoint/test-server"
    - "README has a PubSub section documenting the publisher hierarchy (Connection -> WriterGroup -> DataSetWriter -> PublishedDataSet), the subscriber hierarchy (Connection -> DataSetReader), the full SUB-02 msg shape (D4-09), the UDP-only-UADP rule (D4-03), and the multicastInterface NIC-selection caveat"
    - "Human-verify confirms all three flows import into a running Node-RED and deploy with zero errors"
  artifacts:
    - path: "examples/10 - PubSub UDP-UADP Loopback.json"
      provides: "Self-contained UDP-UADP loopback example (connection + publisher + subscriber + inject + debug + tab)"
      contains: "opcua-pubsub-connection"
      min_lines: 60
    - path: "examples/11 - PubSub MQTT-UADP.json"
      provides: "MQTT transport with UADP encoding example against mqtt://localhost:1883"
      contains: "opcua-publisher"
      min_lines: 60
    - path: "examples/12 - PubSub MQTT-JSON.json"
      provides: "MQTT transport with JSON encoding example against mqtt://localhost:1883"
      contains: "opcua-subscriber"
      min_lines: 60
    - path: "test/example-flows.test.js"
      provides: "Mocha suite (npm-test-discoverable) statically validating ALL examples/*.json — parse, tab presence, node-type presence, config-node reference integrity; explicit PubSub coverage for 10-12"
      min_lines: 80
    - path: "test/run-examples.js"
      provides: "Live runner extended so PubSub flows (no opcua-endpoint) are handled without the endpoint-patch assumption"
    - path: "README.md"
      provides: "PubSub documentation section (DOC-02): config hierarchy, msg shape, UDP-UADP rule, multicastInterface caveat"
      contains: "PubSub"
  key_links:
    - from: "examples/10 - PubSub UDP-UADP Loopback.json"
      to: "opcua-publisher / opcua-subscriber node config property names"
      via: "JSON property keys match the editor `defaults` of plans 04-01 / 04-02"
      pattern: "\"connection\""
    - from: "examples/11 - PubSub MQTT-UADP.json"
      to: "opcua-pubsub-connection transportType=mqtt"
      via: "connection node sets transportType:'mqtt', brokerUrl:'mqtt://localhost:1883'"
      pattern: "mqtt://localhost:1883"
    - from: "test/example-flows.test.js"
      to: "examples/*.json"
      via: "fs.readdirSync(examples).filter(.json) + JSON.parse + node-type assertions"
      pattern: "opcua-publisher"
    - from: "README.md"
      to: "msg shape (D4-09) + multicastInterface caveat (D4-03)"
      via: "PubSub section text"
      pattern: "multicastInterface"
---

<objective>
Ship the discoverability layer for OPC UA PubSub (DOC-01 + DOC-02): three importable example
flows plus a README section. The three flows cover exactly the three shipped transport x encoding
combinations — UDP-UADP (self-contained loopback), MQTT-UADP, and MQTT-JSON — so a Node-RED user
can import a working PubSub pipeline without writing function nodes. A new npm-test-discoverable
Mocha suite statically validates every bundled example (including the three new ones), and the
existing live `test/run-examples.js` runner is extended so the PubSub flows (which have no
`opcua-endpoint`/test-server dependency) run without the endpoint-patch assumption.

Purpose: PubSub is only useful if a user can find a runnable starting point. Flow 10 must work with
zero external infrastructure (publisher and subscriber loopback on the same multicast group). Flows
11/12 demonstrate broker-mediated PubSub and document the broker prerequisite. The README PubSub
section makes the configuration hierarchy, the SUB-02 msg shape, the UDP-only-UADP rule, and the
multicast NIC-selection caveat explicit so users do not fall into the known pitfalls.

Output: `examples/10 - PubSub UDP-UADP Loopback.json`, `examples/11 - PubSub MQTT-UADP.json`,
`examples/12 - PubSub MQTT-JSON.json`, `test/example-flows.test.js` (static validation),
an extended `test/run-examples.js`, and a new README PubSub section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-publisher-subscriber-tests-examples/04-CONTEXT.md
@.planning/phases/04-publisher-subscriber-tests-examples/04-RESEARCH.md
@nodes/opcua-pubsub-connection.html
@examples/04 - Subscribe to Changes.json
@examples/08 - Server with Variables.json
@test/run-examples.js
@README.md

<interfaces>
<!-- CONFIG PROPERTY NAMES the example-flow JSON must use. -->
<!-- Source of truth: plans 04-01 (opcua-publisher) and 04-02 (opcua-subscriber). -->
<!-- PARALLEL-AVAILABILITY CAVEAT: if 04-01-PLAN.md / 04-02-PLAN.md exist when this plan -->
<!-- executes, READ THEIR <interfaces>/editor `defaults` blocks and use those EXACT keys. -->
<!-- If they do not exist yet, derive from CONTEXT.md D4-02..D4-10 + the connection node -->
<!-- HTML defaults below, then RECONCILE on execute. Do NOT invent property names. -->

<!-- opcua-pubsub-connection (config node) — already shipped; defaults from nodes/opcua-pubsub-connection.html: -->
```
type: "opcua-pubsub-connection"
{
  name, transportType: "udp"|"mqtt",
  // UDP:
  multicastGroup: "239.0.0.1", multicastInterface: "0.0.0.0", port: 4840, mtu: 1400,
  // MQTT:
  brokerUrl: "mqtt://localhost:1883", topicPrefix: "ua", qos: 1,
  // identity:
  publisherIdType: "String"|"UInt16"|"UInt32"|"UInt64", publisherId: "<uuid>",
  // cert placeholders (leave ""):
  certificateFile, privateKeyFile, caCertificateFile
}
// credentials block (userName/password) is NOT stored in the flow JSON — leave it out.
```

<!-- opcua-publisher (04-01) — derive from CONTEXT D4-02..D4-06 + lib/pubsub-config.js factories: -->
```
type: "opcua-publisher"
{
  connection: "<opcua-pubsub-connection node id>",   // D4-02
  messageEncoding: "uadp"|"json",                     // D4-03 (UDP => MUST be "uadp")
  publishMode: "acyclic"|"cyclic",                    // D4-06 (use "acyclic" in examples)
  // WriterGroup (one), DataSetWriter (one), PublishedDataSet (one) — D4-04.
  // EXACT editor key names come from 04-01. Expected shape per lib/pubsub-config.js:
  //   WriterGroup:      writerGroupId (1..65535), publishingInterval (>0), keepAliveTime?,
  //                     maxNetworkMessageSize?, priority?
  //   DataSetWriter:    dataSetWriterId (1..65535), keyFrameCount?, dataSetFieldContentMask?
  //   PublishedDataSet: name (or dataSetName), fields: [{ name, dataType }]  // dataType e.g. "Double"/"String"/"Int32"
}
// Input contract (D4-05): msg.payload = { <fieldName>: <rawValue> } — drives the inject node.
```

<!-- opcua-subscriber (04-02) — derive from CONTEXT D4-07..D4-09 + DataSetReader factory: -->
```
type: "opcua-subscriber"
{
  connection: "<opcua-pubsub-connection node id>",   // D4-02
  messageEncoding: "uadp"|"json",                     // D4-03 (MUST match the publisher's)
  // DataSetReader filter — at least ONE of these (D4-08). Match the publisher's identity:
  publisherId?, writerGroupId?, dataSetWriterId?
}
// Output (D4-09): msg.payload (field map), msg.publisherId, msg.writerGroupId, msg.dataSetWriterId,
//   msg.sequenceNumber, msg.timestamp, msg.statusCode, msg.encoding, msg.transport, msg.topic (MQTT only).
```

<!-- Example-flow JSON shape (from examples/04 and examples/08): an array of node objects. -->
<!-- One tab node { id, type:"tab", label, info } (info = markdown shown in the import dialog). -->
<!-- Worker/inject/debug nodes carry z:<tabId>, x/y coords, wires:[[...]]. Config nodes -->
<!-- (opcua-pubsub-connection) have NO z and NO x/y (same as opcua-endpoint in examples/04). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the three PubSub example flows (DOC-01)</name>
  <files>examples/10 - PubSub UDP-UADP Loopback.json, examples/11 - PubSub MQTT-UADP.json, examples/12 - PubSub MQTT-JSON.json</files>
  <read_first>
    - .planning/phases/04-publisher-subscriber-tests-examples/04-01-PLAN.md and 04-02-PLAN.md IF THEY EXIST — read their editor `defaults` / `<interfaces>` blocks and use the EXACT property keys for opcua-publisher / opcua-subscriber. If absent, use the derived names in this plan's <interfaces> and reconcile.
    - nodes/opcua-publisher.html and nodes/opcua-subscriber.html IF THEY EXIST — the `defaults` block is the authoritative property-name source; prefer it over any derived names.
    - nodes/opcua-pubsub-connection.html lines 30-50 (connection-node `defaults`: transportType, multicastGroup, multicastInterface, port, mtu, brokerUrl, topicPrefix, qos, publisherIdType, publisherId, cert placeholders).
    - examples/04 - Subscribe to Changes.json (config-node-without-z + tab + inject + debug shape, info-text style).
    - examples/08 - Server with Variables.json (multi-inject layout, comment nodes, inject `props` array shape).
    - .planning/phases/04-publisher-subscriber-tests-examples/04-CONTEXT.md D4-03 (UDP=UADP only), D4-05 (publisher input msg.payload = { fieldName: rawValue }), D4-08/D4-09 (subscriber filter + output), D4-14 (the three flow filenames).
  </read_first>
  <action>
    Create three Node-RED flow JSON files (each a top-level JSON ARRAY of node objects).
    Use 4-space indentation, double quotes, and the same node-object conventions as
    examples/04 and examples/08. Give every node a stable, file-prefixed id (e.g. `ex10-conn`,
    `ex11-pub`) so ids are unique across all bundled examples. Config nodes
    (opcua-pubsub-connection) have NO `z` and NO `x`/`y`; worker/inject/debug/comment nodes have
    `z: "<tabId>"`, `x`/`y` coordinates, and `wires`.

    CRITICAL — property names: every property key on the opcua-publisher and opcua-subscriber
    nodes MUST match the real editor `defaults` from plans 04-01 / 04-02 (or the shipped
    nodes/opcua-publisher.html / opcua-subscriber.html if present). Do NOT invent keys. If you
    must derive (plans not yet written), use this plan's <interfaces> block and note the
    reconciliation in the SUMMARY. The connection-node keys are already fixed by the shipped
    nodes/opcua-pubsub-connection.html.

    --- examples/10 - PubSub UDP-UADP Loopback.json (self-contained, NO external infra) ---
    Nodes:
    - tab `ex10-tab`: label "10 - PubSub UDP-UADP Loopback". info markdown: explain this is a
      fully self-contained loopback — publisher and subscriber share ONE opcua-pubsub-connection
      on multicast group 239.0.0.1:4840, so it runs on a single host with no broker and no test
      server. State: "Click the inject to publish one DataSet; the subscriber decodes it and the
      debug node shows the msg." Note the UDP-only-UADP rule. Note the multicastInterface caveat
      (leave 0.0.0.0 unless on a multi-NIC host).
    - one opcua-pubsub-connection `ex10-conn` (no z): transportType "udp", multicastGroup
      "239.0.0.1", multicastInterface "0.0.0.0", port 4840, mtu 1400, publisherIdType "String",
      publisherId a fixed UUID literal (e.g. "10000000-0000-4000-8000-000000000010"),
      certificateFile/privateKeyFile/caCertificateFile "".
    - one opcua-publisher `ex10-pub` (z: ex10-tab): connection "ex10-conn",
      messageEncoding "uadp", publishMode "acyclic", one WriterGroup (writerGroupId 1,
      publishingInterval 1000), one DataSetWriter (dataSetWriterId 1, keyFrameCount 1), one
      PublishedDataSet (name "DemoDataSet", fields [{name:"Temperature",dataType:"Double"},
      {name:"Label",dataType:"String"}]). wires: [] (publisher is a sink — it sends to the wire,
      not to another node).
    - one inject `ex10-inject` (z: ex10-tab): name "Publish DataSet". props that set
      `msg.payload` to a JSON object matching the published fields, e.g. a single prop
      `{ "p":"payload", "vt":"json", "v":"{\"Temperature\":23.5,\"Label\":\"sensor-1\"}" }`
      (this is the D4-05 input contract). wires: [["ex10-pub"]].
    - one opcua-subscriber `ex10-sub` (z: ex10-tab): connection "ex10-conn",
      messageEncoding "uadp", and a filter matching the publisher — set writerGroupId 1 and
      dataSetWriterId 1 (and/or publisherId equal to ex10-conn's publisherId). wires:
      [["ex10-debug"]].
    - one debug `ex10-debug` (z: ex10-tab): name "Received DataSet", complete "true",
      targetType "full", tosidebar true (mirror examples/04 debug node fields).
    - optional comment nodes for layout (like examples/08) explaining the loopback.

    --- examples/11 - PubSub MQTT-UADP.json (broker required) ---
    Same structure as flow 10 EXCEPT the connection node `ex11-conn` uses transportType "mqtt",
    brokerUrl "mqtt://localhost:1883", topicPrefix "ua", qos 1, publisherId a distinct fixed UUID.
    publisher `ex11-pub` and subscriber `ex11-sub` both messageEncoding "uadp". tab info markdown
    MUST state: "Requires a local MQTT broker (e.g. Mosquitto) reachable at mqtt://localhost:1883.
    Start one before deploying — e.g. `docker run -p 1883:1883 eclipse-mosquitto`." Keep the same
    DemoDataSet fields, inject payload, and debug node.

    --- examples/12 - PubSub MQTT-JSON.json (broker required, JSON encoding) ---
    Same as flow 11 EXCEPT messageEncoding "json" on BOTH publisher `ex12-pub` and subscriber
    `ex12-sub`; connection `ex12-conn` transportType "mqtt", brokerUrl "mqtt://localhost:1883",
    distinct fixed UUID publisherId. tab info markdown states the same broker requirement AND notes
    that JSON encoding is the cloud-friendly, self-describing combination (no metadata pre-exchange).

    Validate each file parses as JSON before finishing (the Task-2 harness + npm test will enforce
    this, but do a quick `node -e "JSON.parse(require('fs').readFileSync(<path>,'utf8'))"` per file).
  </action>
  <verify>
    <automated>node -e "['10 - PubSub UDP-UADP Loopback','11 - PubSub MQTT-UADP','12 - PubSub MQTT-JSON'].forEach(f=>{const a=JSON.parse(require('fs').readFileSync('examples/'+f+'.json','utf8'));const t=a.map(n=>n.type);['tab','opcua-pubsub-connection','opcua-publisher','opcua-subscriber','inject','debug'].forEach(x=>{if(!t.includes(x))throw new Error(f+' missing '+x)});});console.log('flows 10-12 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All three files parse as JSON arrays (no syntax errors).
    - Each contains exactly one `tab`, one `opcua-pubsub-connection`, one `opcua-publisher`, one `opcua-subscriber`, at least one `inject`, and at least one `debug`.
    - Flow 10 connection transportType is "udp"; flows 11 and 12 are "mqtt" with brokerUrl "mqtt://localhost:1883".
    - Flow 10 and 11 publisher/subscriber messageEncoding is "uadp"; flow 12 is "json".
    - No opcua-publisher/opcua-subscriber node uses a UDP connection with messageEncoding "json" (D4-03 — there is no UDP-JSON flow).
    - Every opcua-publisher/opcua-subscriber `connection` value equals the id of the opcua-pubsub-connection node in the same file.
    - Every property key on the publisher/subscriber nodes matches the real editor `defaults` from 04-01/04-02 (verified by reading those plans/HTML; reconciliation noted in SUMMARY if derived).
    - Flows 11 and 12 tab `info` text mentions the MQTT broker requirement; flow 10 tab `info` mentions it is self-contained / no external infra.
  </acceptance_criteria>
  <done>
    Three PubSub example flows exist under examples/, each a valid Node-RED flow array wiring
    connection + publisher + inject + subscriber + debug, covering UDP-UADP, MQTT-UADP, and
    MQTT-JSON with correct property names and accurate broker/no-infra info text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Static example-flow validation suite (npm test) + extend live run-examples.js</name>
  <files>test/example-flows.test.js, test/run-examples.js</files>
  <read_first>
    - test/run-examples.js (the whole file — especially line 29 `fs.readdirSync(...).filter('.json').sort()` and lines 64-86 `loadAndPatchExamples()` which assumes opcua-endpoint + endpointUrl localhost:4841 patching).
    - examples/04 - Subscribe to Changes.json and the three new flows from Task 1 (the node-type set each must contain).
    - One existing *.test.js under test/ (any) to match the project's Mocha + assert/sinon style ("use strict", double quotes, 2-space indent per CONTEXT constraints).
    - package.json line 28 (`"test": "mocha \"test/**/*.test.js\" --timeout 30000 --exit"`) — the glob auto-discovers new *.test.js files; no package.json change needed.
  </read_first>
  <action>
    Two changes — one new npm-test-discoverable Mocha file, and an extension to the live runner.

    (A) NEW: test/example-flows.test.js — static validation that runs under `npm test` (no
    Node-RED runtime, no network). Pattern: `const fs=require("fs"), path=require("path");
    const assert=require("assert");` then read every file in `examples/` ending in `.json`.

    For EVERY example flow assert:
    - JSON.parse succeeds and yields an Array.
    - Contains exactly one node with `type === "tab"` and that tab has a non-empty `label`.
    - Every node has a `type` (string) and a unique `id` (no duplicate ids within the file).
    - Every property referencing another node by id (e.g. `connection`, `endpoint`, and each
      entry in `wires[][]`) resolves to a node `id` that exists in the SAME file (referential
      integrity — catches a broken `connection` link).

    Additional EXPLICIT assertions for the three PubSub flows (10, 11, 12), driven by a small
    table `[{file, transport, encoding}]`:
    - Contains one `opcua-pubsub-connection`, one `opcua-publisher`, one `opcua-subscriber`, at
      least one `inject`, at least one `debug`.
    - The connection node `transportType` matches the table (`udp` for 10, `mqtt` for 11/12).
    - The publisher and subscriber `messageEncoding` match the table (`uadp` for 10/11, `json`
      for 12) AND are identical to each other.
    - UDP-only-UADP rule (D4-03): assert NO node combines a udp connection with
      messageEncoding "json".
    - Both worker nodes' `connection` equals the in-file opcua-pubsub-connection id.

    Also keep a guard that the full examples set is non-empty and includes the nine pre-existing
    flows plus the three new ones (count >= 12), so a future accidental deletion is caught.

    Use describe/it blocks named per file so failures point at the offending example.

    (B) EXTEND: test/run-examples.js (the LIVE runner used with a running Node-RED — NOT part of
    `npm test`). It auto-globs all examples/*.json (line 29) so flows 10-12 are already picked up,
    but `loadAndPatchExamples()` (lines 64-86) assumes every example has an opcua-endpoint with a
    localhost:4841 endpointUrl to patch. PubSub flows have NO opcua-endpoint, so the patch loop is
    a harmless no-op for them — verify that and add a short comment documenting it. Then make the
    runner PubSub-aware:
    - In the per-example meta, record `transports` = the set of opcua-pubsub-connection
      `transportType` values present (so the summary can label a flow "udp"/"mqtt").
    - For flows whose connection transportType is "mqtt" (11, 12), the live runner cannot succeed
      without a broker; print an informational `dim()` note ("requires MQTT broker at <brokerUrl>")
      and do NOT count a missing-broker failure the same as a flow defect — at minimum keep the
      existing pass/fail accounting but annotate these flows. Flow 10 (udp loopback) should run on
      the same host with no extra infra.
    Keep all existing behavior for flows 01-09 unchanged.
  </action>
  <verify>
    <automated>npx mocha test/example-flows.test.js --timeout 30000 --exit && node -e "JSON.parse(require('fs').readFileSync('package.json'))" && node -c test/run-examples.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx mocha test/example-flows.test.js` exits 0 with all assertions passing for all 12 example flows.
    - The suite FAILS (proven by a scratch run, then reverted) if a publisher/subscriber `connection` id is broken or if a udp flow uses messageEncoding "json".
    - `npm test` total = (prior Phase-4 baseline of 504 passing / 8 pending plus whatever 04-01/04-02/04-03 added) + the new example-flow tests, all green, zero regressions.
    - `node -c test/run-examples.js` reports no syntax error; the runner still globs all examples and runs flows 01-09 exactly as before.
    - run-examples.js records per-example transport info and prints a broker-requirement note for the MQTT flows (11, 12) without breaking flows 01-10.
    - `grep "opcua-publisher" test/example-flows.test.js` returns a match (PubSub node-type coverage is explicit, not incidental).
  </acceptance_criteria>
  <done>
    test/example-flows.test.js statically validates every bundled example (parse, tab, unique ids,
    referential integrity) with explicit PubSub coverage for flows 10-12 and the UDP-only-UADP
    rule, and is auto-discovered by `npm test`. test/run-examples.js is PubSub-aware and still
    runs flows 01-09 unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: README PubSub section (DOC-02) + examples list update</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 60-184 (the "## Nodes" and "## Reference" sections — match heading depth/style and the table conventions used for the existing nodes).
    - README.md lines 222-236 (the "## Examples" numbered list — append 10/11/12).
    - .planning/phases/04-publisher-subscriber-tests-examples/04-CONTEXT.md D4-03 (UDP-only-UADP), D4-09 (exact msg shape), D4-14 (what DOC-02 must contain).
    - nodes/opcua-pubsub-connection.html lines 447-475 (the existing help-panel wording for transport/PublisherId/multicastInterface — reuse phrasing for consistency).
  </read_first>
  <action>
    Add a new top-level "## OPC UA PubSub" section to README.md, placed AFTER the existing
    "## Nodes" section and BEFORE "## Reference" (so PubSub sits alongside the node docs). Write
    in the existing README voice (concise, tables where the existing doc uses tables). Cover:

    1. Intro sentence: PubSub adds broker-less (UDP-UADP multicast) and broker-mediated (MQTT,
       UADP or JSON) publish/subscribe alongside the existing Client/Server nodes. The three
       shipped combinations are UDP-UADP, MQTT-UADP, MQTT-JSON (NO UDP-JSON — D4-03).

    2. "### Configuration hierarchy" — describe the two node-config trees explicitly:
       - Publisher: `opcua-pubsub-connection` -> WriterGroup -> DataSetWriter -> PublishedDataSet
         (one WriterGroup, one-or-more DataSetWriters, each bound to a PublishedDataSet whose
         `fields[]` of `{ name, dataType }` type the outgoing values).
       - Subscriber: `opcua-pubsub-connection` -> DataSetReader (filters on PublisherId /
         WriterGroupId / DataSetWriterId; at least one required).
       A short bullet list or a small tree diagram is fine; match README style.

    3. "### Publisher input" — `msg.payload` is an object keyed by field name
       `{ <fieldName>: <rawValue> }`; one inbound msg produces one outbound NetworkMessage (D4-05).
       Mention acyclic (msg-driven, default) vs cyclic (interval + KeepAlive) modes briefly.

    4. "### Subscriber output (msg shape)" — a table documenting the EXACT D4-09 fields:
       `msg.payload` (field map, values unwrapped), `msg.publisherId`, `msg.writerGroupId`,
       `msg.dataSetWriterId`, `msg.sequenceNumber`, `msg.timestamp`, `msg.statusCode`,
       `msg.encoding` ("uadp"|"json"), `msg.transport` ("udp"|"mqtt"), `msg.topic` (MQTT only).
       Note ConfigurationVersion mismatch surfaces as `node.error()` and is never silently dropped.

    5. "### Encoding rules" — UDP transport MUST use UADP; MQTT allows UADP or JSON; UDP-JSON is
       not a shipped combination (D4-03).

    6. "### UDP multicast NIC selection" — the multicastInterface caveat: the socket always binds
       to `0.0.0.0`; leave `multicastInterface` at `0.0.0.0` to let the OS choose, but on a
       multi-NIC host set it to the host's IP on the PubSub LAN if datagrams are not received
       (reuse the wording from the connection node help panel).

    7. A pointer to the three example flows (10/11/12) and the broker requirement for 11/12.

    Then UPDATE the "## Examples" numbered list (README lines ~227-234) to append (also add #9
    "Session Retry Test" if it is missing — the current list stops at 8):
    - 10. **PubSub UDP-UADP Loopback** — self-contained, no external infra
    - 11. **PubSub MQTT-UADP** — requires a local MQTT broker
    - 12. **PubSub MQTT-JSON** — requires a local MQTT broker
    Keep the existing "All examples work without function nodes." line.
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('README.md','utf8');['## OPC UA PubSub','multicastInterface','msg.publisherId','msg.encoding','UDP','UADP','JSON','DataSetReader','PublishedDataSet','PubSub MQTT-UADP','PubSub MQTT-JSON','PubSub UDP-UADP'].forEach(s=>{if(!r.includes(s))throw new Error('README missing: '+s)});console.log('README PubSub section OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `## OPC UA PubSub` section exists between "## Nodes" and "## Reference".
    - Documents BOTH config hierarchies (Connection -> WriterGroup -> DataSetWriter -> PublishedDataSet; Connection -> DataSetReader).
    - A msg-shape table lists all D4-09 fields including msg.encoding, msg.transport, and msg.topic (MQTT only).
    - States the UDP-only-UADP rule and the multicastInterface NIC-selection caveat.
    - "## Examples" list includes entries 10, 11, 12 with the broker-requirement note for 11/12.
    - The verify command exits 0 (all required strings present).
  </acceptance_criteria>
  <done>
    README has a complete PubSub section covering the configuration hierarchy, the SUB-02 msg
    shape, the UDP-only-UADP rule, and the multicastInterface caveat, plus the examples list now
    references flows 10-12.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the three example flows import and deploy in a running Node-RED</name>
  <files>examples/10 - PubSub UDP-UADP Loopback.json, examples/11 - PubSub MQTT-UADP.json, examples/12 - PubSub MQTT-JSON.json</files>
  <action>Pause for human verification. Claude has authored the three flows, made `npm test`
    statically validate them, and written the README section. The remaining check — importing each
    flow into a running Node-RED editor and deploying without errors — requires a live Node-RED
    instance with this package installed and (for flows 11/12) a local MQTT broker. Claude cannot
    perform this visual/runtime check; see <how-to-verify>.</action>
  <verify>
    <automated>MISSING — human-verify checkpoint. Verification is the user following the <how-to-verify> steps and replying "approved" or listing the specific import/deploy errors observed. (The static gate already ran: `npx mocha test/example-flows.test.js --exit` passed in Task 2.)</automated>
  </verify>
  <done>User replies "approved" indicating all three flows import and deploy cleanly (flow 10 with no infra; flows 11/12 with a local broker running), OR reports specific errors which the executor fixes before re-checkpointing.</done>
  <what-built>
    Three PubSub example flows under examples/ (10 UDP-UADP loopback, 11 MQTT-UADP, 12 MQTT-JSON),
    a static `npm test` validation suite (test/example-flows.test.js), an extended live runner
    (test/run-examples.js), and a README PubSub section. All automated tests pass. This checkpoint
    confirms the flows actually import and deploy in a real Node-RED editor (the one thing that
    cannot be proven statically).
  </what-built>
  <how-to-verify>
    1. From the repo root: `npm install`, then install this package into a local Node-RED
       (e.g. `npm install /path/to/this/repo` in your ~/.node-red, or run the bundled
       `docker compose up -d` dev stack) and restart Node-RED. Open the editor (typically
       http://localhost:1880, or http://localhost:1881 for the Docker stack).
    2. Confirm the editor sidebar shows the new nodes: opcua-pubsub-connection (config),
       opcua-publisher, opcua-subscriber.
    3. Flow 10 (UDP-UADP loopback — no external infra):
       a. Menu -> Import -> Examples -> node-red-contrib-opcua-suite -> "10 - PubSub UDP-UADP Loopback"
          (or Import -> file and pick `examples/10 - PubSub UDP-UADP Loopback.json`).
       b. The flow imports with NO "unknown node type" errors and a populated config node.
       c. Click Deploy. No deploy errors; the publisher/subscriber status indicators settle
          (idle/connected/subscribed — not red error).
       d. Click the "Publish DataSet" inject. The debug sidebar shows a received msg whose
          msg.payload has the published fields (Temperature, Label), plus msg.encoding "uadp",
          msg.transport "udp", and a sequenceNumber.
    4. Flow 11 (MQTT-UADP) and Flow 12 (MQTT-JSON):
       a. Start a local MQTT broker first, e.g. `docker run -p 1883:1883 eclipse-mosquitto`.
       b. Import each flow, Deploy (no errors), click the inject, and confirm the debug node shows
          the round-tripped DataSet (flow 12 msg.encoding "json", flow 11 "uadp"; both
          msg.transport "mqtt"). msg.topic is present for the MQTT flows.
    5. Confirm the Node-RED log shows no errors referencing opcua-publisher / opcua-subscriber /
       opcua-pubsub-connection during import or deploy of any of the three flows.
  </how-to-verify>
  <resume-signal>Type "approved" if all three flows import and deploy cleanly (flow 10 standalone; flows 11/12 with a broker running), OR describe the specific import/deploy/runtime errors observed (e.g. "flow 11 publisher shows red 'error' status" or "unknown property X on opcua-subscriber").</resume-signal>
</task>

</tasks>

<verification>
- `node -e` flow shape check (Task 1 verify) confirms each of flows 10-12 is a JSON array containing tab + opcua-pubsub-connection + opcua-publisher + opcua-subscriber + inject + debug.
- `npx mocha test/example-flows.test.js --exit` exits 0 — all 12 bundled flows parse, have a labeled tab, unique ids, intact node references, and the three PubSub flows match their expected transport/encoding (UDP=UADP, MQTT=UADP/JSON; no UDP-JSON).
- `node -c test/run-examples.js` confirms the extended live runner still parses; flows 01-09 behavior is unchanged and PubSub flows are annotated (broker note for 11/12).
- README verify command confirms the PubSub section, the full msg-shape vocabulary, the UDP-only-UADP rule, the multicastInterface caveat, and the three example list entries are all present.
- `npm test` shows zero regressions versus the 504 passing / 8 pending baseline (plus whatever 04-01/04-02/04-03 added) and adds the new example-flow tests.
- Human-verify checkpoint confirms the one thing that cannot be proven statically: all three flows import and deploy in a real Node-RED editor without errors.
</verification>

<success_criteria>
1. DOC-01: three example flows shipped — `examples/10 - PubSub UDP-UADP Loopback.json`, `examples/11 - PubSub MQTT-UADP.json`, `examples/12 - PubSub MQTT-JSON.json` — each a valid, importable, deployable Node-RED flow array.
2. Flow 10 is a self-contained UDP-UADP multicast loopback runnable with no external infrastructure; flows 11/12 use mqtt://localhost:1883 and document the broker requirement in the tab info text.
3. Encoding follows D4-03 exactly: UDP-UADP (10), MQTT-UADP (11), MQTT-JSON (12); no UDP-JSON flow exists.
4. Every publisher/subscriber/connection property name matches the real config schema from plans 04-01/04-02 (reconciled on execute), and every in-flow `connection` reference resolves to the flow's opcua-pubsub-connection node.
5. `npm test` statically validates all 12 example flows via test/example-flows.test.js, with explicit PubSub coverage for 10-12 and the UDP-only-UADP rule; zero regressions.
6. test/run-examples.js (live runner) handles the PubSub flows without the opcua-endpoint patch assumption and still runs flows 01-09 unchanged.
7. DOC-02: README has an OPC UA PubSub section documenting both configuration hierarchies, the full SUB-02 msg shape (D4-09), the UDP-only-UADP rule (D4-03), and the multicastInterface NIC-selection caveat; the examples list references flows 10-12.
8. Human-verify checkpoint passes: all three flows import and deploy in a running Node-RED with zero errors.
9. All commits authored under `blanpa` only — no Co-Authored-By / AI attribution trailer.
</success_criteria>

<output>
After completion, create `.planning/phases/04-publisher-subscriber-tests-examples/04-04-SUMMARY.md`
documenting: the final node-id scheme and property keys used in each of the three flows (and any
RECONCILIATION where derived names from this plan's <interfaces> differed from the shipped
opcua-publisher/opcua-subscriber editor `defaults`), the structure of test/example-flows.test.js
(what it asserts for every flow vs. only for the PubSub flows), the run-examples.js changes (the
PubSub-aware annotations), the exact placement and headings of the new README PubSub section, and
the final `npm test` count (passing / pending) versus the 504 / 8 baseline.
</output>
